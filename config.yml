
# Server Configuration
server:
  port: 3000
  host: "0.0.0.0"

# Database Configuration
database:
  mysql:
    dsn: "root:admin@tcp(localhost:3306)/rellic_m_elysia?charset=utf8mb4&parseTime=True&loc=Local"
  redis:
    addr: "localhost:6379"
    password: ""
    db: 1

